#include "main.h"

/**
 * main - check the code.
 *
 * Return: Always 0.
 */
int main(void)
{
	print_alphabet_x10(void);
	{
		char ch;
		int i;
		for (i=0; i<10; i++)
		{while (ch <= 'z')
			_putchar(ch);
			ch++;
		}
		_putchat('\n')
	}
	return (0);
}
